---
trigger: always_on
---

# 编辑工具失败时的行为准则

如果你的文件编辑/patch 工具调用失败（包括报 "target file is in .gitignore" 或 patch 无法应用）：

1. **禁止**以工具限制为由宣布任务无法完成。
2. 依次尝试替代手段：换一种编辑工具 → 用终端命令写入（heredoc / python）→
   输出完整文件内容并明确标注目标路径，请用户手动保存。
3. 只有在以上手段全部尝试过之后，才允许报告受阻，并附上每种手段的失败原因。
